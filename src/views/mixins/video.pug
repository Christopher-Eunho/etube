mixin video(info)
    div
        h4=info.title
        ul
            li #{info.rating} / 5
            li #{info.commnets} comments
            li #{info.createdAt}
            li #{info.views} views

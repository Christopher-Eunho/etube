extends base.pug

block content
    if errorMessage
        h4 #{errorMessage}
    form(method ="POST")
        div
            h4 Name
            input(name="name", tpye="text", required)
        div
            h4 Email 
            input(name="email", tpye="email", required)
        div
            h4 Username
            input(name="username", tpye="text", required)
        div
            h4 Password
            input(name="password", tpye="password", required)
        div
            h4 Location
            input(name="location", tpye="text", required)
        input(type="submit", value="Join")
        